<h2>Editar livro</h2>

<form action="/edit/save" method="post">
    <input type="hidden" value="{{book.id}}" name="id">
    <div>
        <label for="title">titulo</label>
        <input type="text" name="title" placeholder="digite titulo" id="title" value="{{book.title}}">
    </div>
    <div>
        <label for="pageqty">numero de páginas</label>
        <input type="number" name="pageqty" placeholder="digite o numero de paginas" id="pageqty" value="{{book.pageqty}}">
    </div>
    
    <button type="submit">enviar</button>
</form>